/*public void NuevaPartida(string nombreJugador)
{
    if (SaveManager.Instance.EstaLleno())
    {
        // abrir menú eliminar partidas
        return;
    }

    // Se crea pero se guarda luego
    SaveManager.Instance.CrearNuevaPartida(nombreJugador, "Partida 1");
}
*/